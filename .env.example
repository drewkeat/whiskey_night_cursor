# Database (port 5433 avoids conflict with local Postgres on 5432; matches docker-compose: user=whiskey, password=whiskey, db=whiskey_night)
# When deploying with docker compose, the app service overrides this to use the postgres service.
DATABASE_URL="postgresql://whiskey:whiskey@localhost:5433/whiskey_night?schema=public"

# NextAuth
NEXTAUTH_SECRET="generate-with-openssl-rand-base64-32"
NEXTAUTH_URL="http://localhost:3000"

# Optional: OAuth (Google)
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=
# Expose client ID to browser so login/register can show Google button (use same value as GOOGLE_CLIENT_ID)
# NEXT_PUBLIC_GOOGLE_CLIENT_ID=

# Optional: Web search for adding whiskey
# SEARCH_API_KEY=

# Email (e.g. Resend)
RESEND_API_KEY=
RESEND_FROM_EMAIL="Whiskey Night <noreply@example.com>"

# SMS (Twilio)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=

# Optional: cron for event reminders (call GET /api/notifications/reminders with Bearer CRON_SECRET)
CRON_SECRET=
